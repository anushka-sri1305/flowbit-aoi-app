# Flowbit AOI App (Polished - ZIP distribution)

## Quick start
1. `npm install`
2. `npx playwright install --with-deps` (optional for tests)
3. `npm run dev`
4. Open http://localhost:5173

## Notes
- Node 18+ recommended.
- If WMS tiles show CORS errors, use a proxy for the WMS endpoint during dev.
